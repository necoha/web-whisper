gradio>=4.0.0
numpy>=1.24.3
tqdm>=4.65.0
ffmpeg-python>=0.2.0
scipy>=1.9.0

# macOS Apple Silicon - MLX backend
mlx-whisper==0.4.2; sys_platform == "darwin" and platform_machine == "arm64"

# Windows/Linux - faster-whisper backend  
faster-whisper>=1.0.0; sys_platform != "darwin" or platform_machine != "arm64"
torch>=2.0.0; sys_platform != "darwin" or platform_machine != "arm64" 